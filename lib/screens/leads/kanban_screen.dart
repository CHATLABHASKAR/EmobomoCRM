import 'package:flutter/material.dart';

class KanbanScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Kanban')),
      body: Center(child: Text('Kanban Screen')),
    );
  }
} 