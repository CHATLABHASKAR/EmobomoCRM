import 'package:flutter/material.dart';

class SalesKpiPanel extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Card(child: Padding(padding: EdgeInsets.all(16), child: Text('Sales KPI Panel')));
  }
} 