import 'package:flutter/material.dart';

class KpiCard extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Card(child: Padding(padding: EdgeInsets.all(16), child: Text('KPI Card')));
  }
} 